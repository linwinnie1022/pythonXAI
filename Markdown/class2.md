太好了，你今天學的內容非常豐富，我幫你整理成**國小四年級能看懂的 Python 筆記**，我會用簡單語言、圖像化思考方式，像上課筆記一樣。

---

# 🐍 Python 第三堂筆記（條件、迴圈、Streamlit）

---

## 一、比較運算子（比大小）

👉 比較運算子只能用在**相同種類的資料**上

| 運算子 | 意思     | 範例   | 結果  |
| ------ | -------- | ------ | ----- |
| `==`   | 等於     | 1 == 1 | True  |
| `!=`   | 不等於   | 1 != 1 | False |
| `>`    | 大於     | 2 > 1  | True  |
| `<`    | 小於     | 1 < 2  | True  |
| `>=`   | 大於等於 | 1 >= 1 | True  |
| `<=`   | 小於等於 | 1 <= 2 | True  |

```python
print(1 == 1)  # True
print(1 != 1)  # False
```

---

## 二、邏輯運算子（判斷多個條件）

| 運算子 | 功能             | 範例           | 結果  |
| ------ | ---------------- | -------------- | ----- |
| `and`  | 全部都對才對     | True and False | False |
| `or`   | 只要有一個對就對 | True or False  | True  |
| `not`  | 反過來           | not True       | False |

```python
print(True and False)  # False
print(True or False)   # True
print(not True)        # False
```

---

## 三、運算子優先順序（先做哪個）

1. `()` 括號
2. `**` 次方
3. `* / // %` 乘除取商餘數
4. `+ -` 加減
5. `== != > < >= <=` 比較
6. `not`
7. `and`
8. `or`

---

## 四、if 條件判斷（根據條件做事）

```python
password = "1234"

if password == "1234":
    print("歡迎Jeffrey")
elif password == "5678":
    print("歡迎Tim")
else:
    print("密碼錯誤")
```

### 🧠 小技巧

- `if` 是第一個條件
- `elif` 是其他條件（排除前面已判斷過的）
- `else` 是最後都不符合時的答案

---

## 五、BMI 範例

```python
x = float(input("請輸入身高(m):"))
y = int(input("請輸入體重(kg):"))
bmi = y / x**2

if bmi < 18.5:
    print("過輕")
elif 18.5 <= bmi < 24:
    print("正常")
else:
    print("過重")
```

---

## 六、Streamlit（做網頁程式）

### 1️⃣ 匯入模組

```python
import streamlit as st
```

- `st` 就是小名，方便打字

### 2️⃣ 輸入數字

```python
number = st.number_input("請輸入一個數字:", step=1, min_value=0, max_value=100)
```

- step=1 → 只能輸入整數
- min_value/max_value → 設定範圍

### 3️⃣ 判斷成績等級

```python
if number >= 90:
    st.write("你的等級為A")
elif 80 <= number < 90:
    st.write("你的等級為B")
else:
    st.write("你的等級為F")
```

### 4️⃣ 按鈕練習

```python
if st.button("按我一下"):
    st.balloons()
```

---

## 七、奇偶數判斷

```python
number = st.number_input("請輸入一個整數:", step=1)

if number % 2 == 0:
    st.write("你輸入的是偶數")
else:
    st.write("你輸入的是奇數")
```

- `%` → 取餘數，餘數是0就是偶數

---

## 八、for 迴圈（重複做事）

### 1️⃣ 基本用法

```python
for i in range(5):
    print(i)  # 0 1 2 3 4
```

### 2️⃣ 設定起始值和結束值

```python
for j in range(1, 5):
    print(j)  # 1 2 3 4
```

### 3️⃣ 設定間隔

```python
for i in range(1, 10, 2):
    print(i)  # 1 3 5 7 9
```

### 4️⃣ 小範例：教室號碼

```python
x = int(input())
y = int(input())
for z in range(x, y + 1):
    print(f"{z}號在教室")
```

---

## 九、數字金字塔（Streamlit 版）

```python
st.title("數字金字塔")
number = st.number_input("請輸入一個整數(1到9):", min_value=1, max_value=9, step=1)
st.write("數字金字塔:")

for i in range(1, int(number) + 1):
    st.write(str(i) * i)
```

輸入 5 → 會顯示：

```
1
22
333
4444
55555
```

---

## 🔹 小結

今天你學會了：

1. 比較運算子、邏輯運算子
2. if / elif / else 條件判斷
3. BMI / 成績 / 奇偶數小工具
4. for 迴圈、range
5. Streamlit 基本操作：輸入數字、按鈕、文字
6. 用迴圈做數字金字塔

---
