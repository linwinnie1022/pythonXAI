太好了，你這一整串其實就是**一整個學期的重點** 👍
我幫你整理成一份 **「四年級也看得懂的 Python 筆記」**，用簡單的話、重點清楚、適合考前複習或交作業。

---

# 🐍 Python 程式設計筆記（四年級版）

---

## 一、算數指定運算子（讓數字自己改變）

```python
a = 1
a += 1   # a = a + 1
a -= 1   # a = a - 1
a *= 2   # a = a * 2
a /= 2   # a = a / 2
a //= 2  # a = a // 2
a %= 2   # a = a % 2
a **= 2  # a = a ** 2
```

📌 **記住：**

- `+=` 很常用（加一、加錢、加庫存）
- `-=` 用在扣分、扣庫存

---

## 二、運算的優先順序（誰先算）

1️⃣ `()` 括號
2️⃣ `**` 次方
3️⃣ `* / // %`
4️⃣ `+ -`
5️⃣ 比較運算子：`== != > < >= <=`
6️⃣ `not`
7️⃣ `and`
8️⃣ `or`
9️⃣ `=` `+=` `-=`

📌 **跟數學一樣，先算乘除再算加減**

---

## 三、while 迴圈（一直做，直到條件不成立）

```python
i = 0
while i < 5:
    print(i)
    i += 1
```

📌 **重點**

- 條件是 `True` → 一直跑
- 條件變 `False` → 停止

---

## 四、break（強制跳出迴圈）

```python
while True:
    if i == 3:
        break
```

📌 常用在：

- 猜數字
- 找到答案就停

---

## 五、random 隨機數（抽籤、猜數字）

```python
import random

random.randrange(1, 7)   # 1~6
random.randint(1, 6)    # 1~6（包含6）
```

### 🎯 猜數字遊戲範例

```python
x = random.randrange(1, 101)
while True:
    a = int(input("猜數字"))
    if a < x:
        print("太小")
    elif a > x:
        print("太大")
    else:
        print("答對")
        break
```

---

## 六、字典 dict（像是有名字的盒子）

```python
d = {"a": 1, "b": 2}
```

- key（鑰匙）：`"a"`
- value（值）：`1`

### 常用指令

```python
d.keys()     # 所有key
d.values()   # 所有value
d.items()    # key + value
```

### 新增 / 修改

```python
d["c"] = 3
d["a"] = 10
```

### 刪除

```python
d.pop("a")
```

---

## 七、成績系統（巢狀字典）

```python
grade = {
    "小明": {"國文": [90,80,70], "數學": [85,75,65]}
}
```

📌 可以這樣拿資料：

```python
grade["小明"]["數學"][0]
```

---

## 八、for 迴圈 + dict（算平均）

```python
for name, subjects in grade.items():
    chinese = subjects["國文"]
    avg = sum(chinese) / len(chinese)
```

📌 `sum()`：全部加起來
📌 `len()`：有幾個

---

## 九、Streamlit 基本用法（做網頁）

```python
import streamlit as st
```

### 顯示文字

```python
st.title("標題")
st.write("內容")
```

### 顯示圖片

```python
st.image("image/apple.png", width=150)
```

### 使用者輸入

```python
st.number_input("數字", min_value=1, max_value=5)
```

### 分欄

```python
cols = st.columns(2)
```

### 按鈕

```python
if st.button("購買"):
    st.write("成功")
```

---

## 十、session_state（記住資料）

```python
if "stock" not in st.session_state:
    st.session_state.stock = 10
```

📌 用在：

- 庫存
- 分數
- 購物車

---

## 十一、函數 function（把程式變成工具）

### 最簡單的函數

```python
def hello():
    print("Hello")
```

### 有參數

```python
def hello(name):
    print(name)
```

### 有回傳值

```python
def add(a, b):
    return a + b
```

📌 **return 出現，函數就結束**

---

## 🎉 總結（你已經很厲害了）

你現在已經會：

✅ 迴圈
✅ 條件判斷
✅ random
✅ dict
✅ Streamlit 做網頁
✅ 函數
